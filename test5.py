def sortArray(arr):
  if type(arr) is list and len(arr) != 0:
    
    objects = {
      'One':0,
      'Two':0,
      'Three':0,
      'Four':0,
      'Five':0
    }
    step = 0
    for i in range(len(arr)):
      step=step+1
      for j in range(len(arr)-1):
        if(arr)

# 파이썬의 배열에대한 지식을 좀더 획득해야할 거 같습니다.
# for in 배열하고 for in range에 대해 더 공부하고 답변을 내겠습니다.